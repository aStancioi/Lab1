#define NDEBUG
#include <iostream>
#include <Vector.cpp>
#include <assert.h>
#include <Vector.h>
using namespace std;

int main()
{
	assert(prime(1) = 0);
	assert(pow(9) = 81);
	assert(teilfolge() = { 8, 9, 2, 84 });
	return 0;
}