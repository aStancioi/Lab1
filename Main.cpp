#define NDEBUG
#include <iostream>
#include "Vector.cpp"
#include <assert.h>
#include "Vector.h"
using namespace std;

int main()
{
	assert(prime(1) = 0);
	assert(pow(9) = 81);
	assert(teilfolge({ 1,5,3,4,2,6,9 }) = { 5,3,4,2 });
	return 0;
}